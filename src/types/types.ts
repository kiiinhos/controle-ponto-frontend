export interface Entry {
  userCode: string;
  dateEntry: string;
  hourEntry: string;
  id: number;
}

export interface Exit {
  userCode: string;
  dateExit: string;
  hourExit: string;
  id: number;
}
